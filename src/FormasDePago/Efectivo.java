package FormasDePago;

public class Efectivo extends FormaPago {
    //Esta clase no hace nada pero la dejo primero por si en alguna momento hay que agregar otro requerimiento
    // y segundo mara seguir con la logica que use para modelar (una clase para cada forma de pago)
}
